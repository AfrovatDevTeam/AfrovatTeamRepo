body{
    text-align:center;
    font-family:Arial;
    background:#f4f4f4;
}

/* Game container */
.game-area{
    position:relative;
    width:600px;
    height:400px;
    border:3px solid black;
    margin:30px auto;
    overflow:hidden;
}

/* Background image inside the frame */
.background{
    position:absolute;
    top:0;
    left:0;
    width:100%;
    height:100%;
    object-fit:cover;
    z-index:0;
}

/* Ball */
.ball{
    width:40px;
    height:40px;
    background:red;
    border-radius:50%;
    position:absolute;
    z-index:2;
    animation: ballMove 6s linear infinite;
}

/* Ball movement + explosion moment */
@keyframes ballMove{
    0%{left:40px; top:40px; transform:scale(1);}
    25%{left:200px; top:100px;}
    45%{left:280px; top:300px;}
    50%{
        left:300px;
        top:350px;
        background:orange;
        transform:scale(2);
        opacity:0.6;
    }
    55%{
        transform:scale(3);
        background:yellow;
        opacity:0;
    }
    70%{
        left:450px;
        top:200px;
        opacity:1;
        transform:scale(1);
        background:red;
    }
    100%{left:100px; top:50px;}
}

/* Dog */
.dog{
    position:absolute;
    bottom:0;
    font-size:40px;
    z-index:2;
    animation: dogRun 6s linear infinite;
}

/* Dog movement */
@keyframes dogRun{
    0%{left:0px;}
    25%{left:150px;}
    50%{left:300px;} /* catches the ball */
    70%{left:450px;}
    100%{left:100px;}
}

/* Hover catch */
.ball:hover{
    background:green;
    transform:scale(1.4);
    animation-play-state: paused;
}

/* Buttons container */
.buttons{
    width:600px;
    margin:20px auto 0; /* positions buttons right below the game frame */
    display:flex;
    justify-content:space-around;
}

/* Base button style */
.btn{
    padding:12px 25px;
    font-size:18px;
    font-weight:bold;
    border:none;
    border-radius:12px;
    cursor:pointer;
    color:white;
    box-shadow:0 5px 15px rgba(0,0,0,0.3);
    transition: all 0.3s ease;
}

/* Play button */
.btn.play{
    background: linear-gradient(45deg, #4caf50, #81c784);
}

/* Pause button */
.btn.pause{
    background: linear-gradient(45deg, #ffb300, #ffd54f);
}

/* Close button */
.btn.close{
    background: linear-gradient(45deg, #f44336, #e57373);
}

/* Hover effect for all buttons */
.btn:hover{
    transform: translateY(-5px) scale(1.05);
    box-shadow:0 8px 20px rgba(0,0,0,0.4);
}